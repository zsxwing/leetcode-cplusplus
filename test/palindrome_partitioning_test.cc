#include "common.h"
#include "palindrome_partitioning.h"
#include <gtest/gtest.h>

TEST(Palindrome, partition) {
//  Solution s;
//  vector<vector<string> > re;
//  ASSERT_EQ(re, s.partition("cdd"));
}
